@page
@model BikeService.Pages.ServiceTypes.IndexModel
@{
}

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<br /><br />
<div class="border backgroundwhite">
    <div class="row">
        <div class="col-6">
            <h2 class="text-dark">Service Types List</h2>
        </div>
        <div class="col-6 text-right">
            <a asp-page="Create" class="btn btn-warning">
                <i class="fas fa-plus"></i>&nbsp; Create New
            </a>
        </div>
    </div>
    <br />
    <div>
        @if(Model.ServiceTypes.Count==0)
        {
            <p>No Service Type exists. Please create a new service type.</p>
        }
        else
        {
            <table class="table table-striped border">
                <tr class="table-secondary">
                    <th>
                        Name
                    </th>
                    <th>
                        Price
                    </th>
                    <th></th>
                </tr>
                @foreach(var item in Model.ServiceTypes)
                {
                <tr>
                    <td>
                        @item.Name
                    </td>
                    <td>
                        @item.Price
                    </td>
                    <td style="width:150px;">
                        <div class="btn-group" role="group">
                            <a class="btn btn-primary text-white" asp-page="Edit" asp-route-Id="@item.Id">
                                <i class="far fa-edit"></i>
                            </a>
                            <a class="btn btn-success text-white" asp-page="Details" asp-route-Id="@item.Id">
                                <i class="far fa-list-alt"></i>
                            </a>
                            <a class="btn btn-danger text-white" asp-page="Delete" asp-route-Id="@item.Id">
                                <i class="far fa-trash-alt"></i>
                            </a>
                        </div>

                    </td>
                </tr>
                }
            </table>
        }
    </div>
</div>

